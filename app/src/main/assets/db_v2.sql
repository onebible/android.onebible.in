CREATE TABLE `tbl_notes` (`id`    INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, `title` NOT NULL, `description` TEXT NOT NULL, `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP, `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP);